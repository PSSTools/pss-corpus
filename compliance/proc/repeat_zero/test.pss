import std_pkg::*;

component pss_top {
  action A {
    exec body {
      message(NONE, "@@PSS-TRACE act pss_top::A");
      int n = 0;
      repeat (n) {
        message(NONE, "@@PSS-TRACE chk rz.wrong n=%d", n);
      }
      message(NONE, "@@PSS-TRACE chk rz.after n=%d", n);
    }
  }
}
