{
  "schema": "pss-corpus/model/1.0",
  "id": "proc.rt.sign.compare.001",
  "rev": 1,
  "title": "A relational compare with an unsigned operand is an unsigned compare (Example 43, rewritten)",
  "level": "L2",
  "lrm": [
    "8.5.2",
    "8.7.1"
  ],
  "features": {
    "tests": [
      "rt.compare.signedness"
    ],
    "uses": [
      "exec.body",
      "std.message",
      "proc.local.init"
    ]
  },
  "sources": [
    "test.pss"
  ],
  "run": {
    "root": "pss_top::A",
    "seeds": {
      "count": 1
    }
  },
  "expect": {
    "outcome": "ok"
  },
  "types": {
    "pss_top::A": {
      "kind": "action",
      "atomic": true,
      "component": "pss_top",
      "fields": {},
      "constraints": [],
      "body": {
        "seq": [
          {
            "chk": "scmp",
            "eq": {
              "mixed": true,
              "signed": false
            }
          }
        ]
      }
    }
  },
  "derivation": [
    "8.5.2: when one or both operands are unsigned the comparison is between unsigned values.",
    "mixed: y = -10 as bit[32] is 4294967286 > 1, so x < y is true.",
    "signed: both int, -10 < 1, so a < b is false."
  ]
}
