import std_pkg::*;

component pss_top {
  function int twice(int x) {
    return 2 * x;
  }

  action A {
    exec body {
      message(NONE, "@@PSS-TRACE act pss_top::A");
      int r = comp.twice(21);
      message(NONE, "@@PSS-TRACE chk cf r=%d", r);
    }
  }
}
