import std_pkg::*;

component pss_top {
  action A {
    exec body {
      message(NONE, "@@PSS-TRACE act pss_top::A");
      int x = 1;
      {
        int x = 5;
        x = x + 1;
        message(NONE, "@@PSS-TRACE chk inner x=%d", x);
      }
      message(NONE, "@@PSS-TRACE chk outer x=%d", x);
    }
  }
}
