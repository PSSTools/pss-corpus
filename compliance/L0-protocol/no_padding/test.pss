import std_pkg::*;

component pss_top {
  action A {
    exec body {
      message(NONE, "@@PSS-TRACE act pss_top::A");
      bit[8]  a = 5;
      bit[32] b = 5;
      int[16] c = -5;
      bit[64] d = 5;
      message(NONE, "@@PSS-TRACE chk nopad a=%d b=%d c=%d d=%d", a, b, c, d);
    }
  }
}
